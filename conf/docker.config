/*
 * Docker profile. The simulatr container is built and pushed by the
 * package repo (Katsevich-Lab/simulatr) on release tags.
 */

process {
    container = 'ghcr.io/katsevich-lab/simulatr:latest'
}
